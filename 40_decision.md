# 922 / 40 — 决策页与实验计划

---

## 1. 两个问题的结论

### Q1：风格编码的表怎么做，要三层分级吗？

**要三层，但要的是"语义分解"不是"标签细分"。**

| 层级 | 语义 | 通路 | 维度 |
|---|---|---|---|
| **书体 script**（3） | 结体、笔画形态（结构层，与 g 同域） | **调制 g**（FiLM），不进 adaLN | 64 |
| **书家 callig**（45） | 笔法、墨色、粗细（全局） | **adaLN**（主通道） | 128 |
| **pair 残差**（87） | 该书家在该书体下的具体面貌 | 加到书家向量上，**可置零回退** | 128 |
| 子风格 K（可选） | 同一 pair 内多模态 | mean→adaLN 或 CA | 4×384 |

**为什么值得做**（不是指标，是能力）：
- 稀疏 pair（15 个 <50 样本）**训练时自动回退**到主效应
- few-shot 新书家**只需学 128 维主效应行**（现在要学 1536 维、还混杂书体交互）
- 未见 (书家, 书体) 组合**可以合成**（现在词表里没有 = 完全不能）

**⚠ 前置依赖**：`script_id` 目前**完全没用过**；而"表里的向量从哪来"还没解决
（DINO 质心余弦 0.956）。**必须先等 `v15b_supcon` 终值**。

### Q2：注入要把 adaLN 换成 cross-attn 吗？

**不换。**

```
效应量：  注入方式之间 ≤ 0.008    ← 噪声级
          表 / 数据之间 0.022+
          大杠杆        0.165
成本：    xattn +33%（batch 360 → 240，显存 21.3G）
结果：    xattn 独立口径最差（ink_ssim 0.3562 vs v13 0.3872）
语义：    风格 = 全局属性 → adaLN；内容 g = 空间 → xattn/FiLM
```

**要做的是"加"不是"换"**：
- **B1** 书体 FiLM on `g_tok`（~50K，zero-init 可回退）
- **B2** 风格 FiLM on `g_tok`（~98K，比 CA 便宜 3–5 倍）
- 顺手修 4 个注入层的梯度不平衡（L3 吃 60%）

⚠ **"xattn 无用"是口径污染**（CFG 下 g 被结构性抵消 + Q 无位置编码），
不是 xattn 本身没用 → 组件结论必须改用 **cfg=1.0** 报告。

---

## 2. 优先级（按 期望收益 / 成本 排序）

| 优先级 | 动作 | 成本 | 为什么排这里 |
|---|---|---|---|
| **P0** | 统一口径 + 补齐待查数字 | 0.5 天，不训练 | 后面所有判断都建立在可信数字上 |
| **P1** | 诊断 D1–D4 + T0–T2 | 1 天，不训练 | 可能直接省掉一次 150k 步的错误投入 |
| **P2** | 三层语义分解表 S2 | 2–3 天（1 次 150k） | 本轮主线 |
| **P3** | few-shot 在 S2 上重跑 | 1 天 | 直接验证"新书家只需学 128 维" |
| **P4** | 实例风格编码器（方向 C） | 大模型改动 | **仅当 P2/P3 证明"书家 ID 监督已用尽"** |

---

## 3. P0：先把数字弄干净（0.5 天，不训练）

| # | 任务 | 现状 |
|---|---|---|
| P0-1 | 查 **v15c 的 0.126 差异**（训练内 0.6961 vs 独立 0.5697） | 设备差异方向反了，未查清 |
| P0-2 | 拿 **`v15b_supcon` 终值**（tmux `v15bs`） | ⚠ **SSH 连不上**，先确认 GPU 状态 |
| P0-3 | **统一口径重测**全部已有 ckpt（v13_base / wd01 / 12ch_post / v15a / v15b / v15b_supcon / v15c） | 用 `ink_ssim` + **cfg=1.0** + EMA 一致 |
| P0-4 | 确认 v15a 的两个冲突值（0.5699 vs 0.5488）来源 | 训练内 vs 独立口径？ |
| P0-5 | 续跑 v13_wd01（停在 125k） | 唯一确认有效的增益没吃满 |

⚠ P0-3 的**输出必须是一张同口径表**，否则后面的实验无法判读。

---

## 4. P1：诊断（1 天，不训练，收益最高）

### 4.1 注入侧

| # | 诊断 | 读什么 |
|---|---|---|
| **D1** | `‖y_emb‖ / ‖t_emb‖` + `y_emb=0` 时 adaLN 输出的变化量 | 若 ≪1 → **风格被 timestep 淹没**，`ratio_style` 低有了具体且便宜的修法 |
| **D2** | cfg=1.0 重测 v13/v15a/v15b/v15c | 拆掉 CFG 抵消污染，重判注入方式 |
| **D3** | 换书家的差异热力图（均匀？还是集中在笔画/起收笔？） | 决定要不要空间化（B2 / CA） |
| **D4** | `ratio_style` 同口径重测（cfg=1.0，≥100 对） | 现在的 1.21 / 1.42 不可比 |

### 4.2 风格编码侧

| # | 诊断 | 读什么 |
|---|---|---|
| **T0** | 可训表（v15）终值 vs 初始化的变化量 | 梯度到底有没有到表 |
| **T1** | 换书家生成的**笔画级统计量**差异（墨色直方图 / 笔画宽度 / 边缘锐度） | 风格有没有真的进图 |
| **T2** | ★ **风格可分性探针**：生成图 DINO 特征 → 87/45 类分类器 top-1 | 随机基线 1.1%（87 类）。**≈基线 = 生成的书家根本不可分** |

**T2 是最推荐的主判据**：跨 ckpt 可比、不需要同 cfg、直接回答"能不能认出是谁写的"。

---

## 5. P2：三层语义分解表 S2（主线实验）

### 5.1 配置

```
基线：    v13_base_50k.json（strict 0.5703 / ink_ssim 0.3872）
变量 1：  y_callig_embedder: LabelEmbedder(45,128) → HierStyleEmbedder
            E_script(3,64) + E_callig(45,128) + E_pair(87,128)×α(init 0.1)
变量 2：  script_film on g_tok（zero-init）
其余：    完全不变（REPA 0.03 / factorized_cat / glyph_vec_cond / adaln×4 / wd / batch 360）
```

### 5.2 消融阶梯（**必须逐级，不要一次全上**）

| 步 | 配置 | 回答什么 | 成本 |
|---|---|---|---|
| S2-0 | 基线重跑（同 seed 复现） | 确认可比 | 1× |
| S2-a | **只加书体 FiLM**（`E_script` + film） | 书体层有没有用？（与 g 冗余的风险） | 1× 150k |
| S2-b | **只改表为分解式**（`E_callig` + `α·E_pair`，无书体） | 残差分解有没有用？ | 1× 150k |
| S2-c | a + b（完整 S2） | 组合效果 | 1× 150k |

⚠ 每级 150k 步 ≈ 9.5h（4.1 step/s @ batch 360）。**串行全程 ~28h。**
若 GPU 紧张，S2-a 可以缩到 60k 步做趋势判读（v14 的 60k→120k 斜率是可靠的）。

### 5.3 判据（**不看 strict ssim**）

| 判据 | 基线 | 目标 | 判定 |
|---|---|---|---|
| ★ **风格可分性探针 T2** | 待测 | **> 20%**（45 类） | 主判据 |
| ★ **few-shot 新书家 Δssim** | +0.010~+0.038 | **≥ +0.05**（冻结主干） | 主判据 |
| `ratio_style`（cfg=1.0，≥100 对） | 1.21–2.48 | **≥ 2.5** | 副 |
| `ink_ssim`（strict 249） | 0.3898 | 不能变差 | 兜底 |

> **如果 S2 在 strict ssim 上零增益、但 T2 / few-shot 明显涨 —— 这就是成功。**
> 反过来若只有 ssim 涨而 T2 不动，那是噪声。

---

## 6. P3：few-shot 重跑（验证分解的价值）

在 S2 的 ckpt 上重跑 5 个现有主题（沈周-行 / 伊秉绶-行 / 傅山-行 / 徐渭-行 / 伊秉绶-隶）：

| 对比臂 | 现状（87 平表） | S2（分解表） |
|---|---|---|
| 新行维度 | 1536 | **128**（`E_callig`），`E_pair` 留零 |
| 初始化 | `mean_scaled` / `row_pt` | `E_callig` 用该书家 DINO 质心；`α·E_pair` = 0 |
| 预期 | 0.5190 → 0.5571（沈周） | **≥ 0.56 且收敛更快** |

判据：**配对 t 检验**（`_sync_work/fs6_paired.py`），不是两个均值相减（n=100 独立 SE≈0.02）。

---

## 7. 不做清单（明确写下来，防止反复）

| ✗ 不做 | 理由 |
|---|---|
| 继续细分标签粒度（45→87→更多） | v14 + v15a 两次证伪，零增益 |
| 加大/减小模型容量 | strict 全落 0.51–0.57；缩深度 −0.026 |
| 数据增广 | −0.007 |
| 结构损失混入扩散损失 | 0.731 vs 0.330，严重有害 |
| 12ch **从头**训 | −0.056（只有 4ch 预训练 + 后训练扩 12ch 有效） |
| 全解冻（fullft） | gap 0.195 = 三代最差 |
| **把 adaLN 换成 xattn** | 效应量 ≤0.008，成本 +33%，独立口径最差 |
| 给风格表加 DINO 锚定 | DINO pair 质心余弦 0.956，锚定只会往"无区分"拉 |
| 加更多子风格 token（K=8/16） | K=4 已零增益，且 K token 间余弦 0.884（模态是假的） |
| 用 strict ssim 作为风格实验的主判据 | 已饱和，效应量 < 0.01 |

---

## 8. 风险登记

| 风险 | 概率 | 影响 | 应对 |
|---|---|---|---|
| **SSH / GPU 不可用** | 当前已发生 | 所有实验停摆 | P0/P1 全是不训练的诊断，可先在已落盘的图上做 |
| **`v15b_supcon` 终值也不涨** | 中 | 说明"书家 ID 监督已用尽" → S2 大概率也无效 | **直接跳方向 C（实例编码器）**，不再在表上花时间 |
| 书体层与 g 冗余 → 无效 | 中 | S2-a 白跑 | S2-a 单独做，无效就砍掉这层，S2 退化成 b |
| 训练集改了 229+751 条未验证 | 高 | 所有新实验与历史不同数据 | **要么先重训基线，要么新实验全部用 fixed 数据并注明** |
| 两个 eval 口径差 0.126 未查清 | 高 | 判读不可信 | **P0-1 必须先解决再开新实验** |

---

## 9. 一句话总结

> **风格是这个项目唯一明确的短板**（`ratio_style` 1.2–2.5 vs 理想 5–10），
> 但**短板不在注入方式**（效应量 ≤0.008），而在**"表里的向量有没有信息"**
> （DINO 质心余弦 0.956）。
> **先把表的信息源解决了，再用"语义分解"而不是"标签细分"重构表，
> 注入保持 adaLN 主通道 + 加廉价的 FiLM 旁路。
> 判据换成风格可分性探针和 few-shot 上限 —— 不要再用已饱和的 strict ssim。**
