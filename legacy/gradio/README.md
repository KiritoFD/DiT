# legacy/gradio — 已归档的 Gradio 演示

2026-09-17 从仓库根目录移入。

| 文件 | 说明 |
|---|---|
| `gradio_fame_local.py` | 本地 Gradio 演示（依赖已废弃的 ControlNet 线） |
| `gradio_stdskel.py` | 标准骨架 Gradio 演示。**0 引用** |

⚠ 这两个文件**在归档前就已经有语法错误**（早前某次全局字符串替换留下的，
如 `def data/skel/std_skel(...)`），并非本次移动造成。作为历史留存。
