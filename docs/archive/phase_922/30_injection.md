# 922 / 30 — 注入机制：adaLN 要不要换成 cross-attn

> 本文只回答注入方式。风格表的形态见 [`20_style_encoding.md`](20_style_encoding.md)。

---

## 0. 结论先行

| 问题 | 回答 |
|---|---|
| adaLN 换成 cross-attn 吗？ | **不换。** |
| 理由（一句话） | **adaLN 与 xattn 承载的语义不同**：风格是**全局属性**（笔法/墨色/粗细整字一致）→ adaLN 语义正确、成本为零、已验证；xattn 是**空间/内容寻址**，适合 `g`（内容），对风格是过参数化。 |
| 量化依据 | 三种注入方式的效应量 **≤ 0.008（噪声级）**，而表/数据的效应量 **0.022+**；xattn 成本 **+33%** 且独立口径下**最差**。 |
| 那要做什么？ | **加**一条廉价的"风格调制骨架"（FiLM on `g_tok`，zero-init 可回退），而不是换掉 adaLN。 |
| 必须先看的一件事 | "xattn 无用"的历史结论**有口径污染**（CFG 下 g 被结构性抵消），不是 xattn 本身没用 —— 见 §2。 |

---

## 1. 现在的五条注入通路

| # | 通路 | 机制 | 状态 | 梯度占比 |
|---|---|---|---|---|
| ① | **adaLN（全局）** | `c = t_emb + y_emb` → 每 block 6 个调制参数（shift/scale/gate × attn/mlp） | **主通道**，v13 用它拿最优 | — |
| ② | **g 的输入层 token-add** | `x = x + glyph_scale · g_tok` | **必需**（置零 ssim −0.28） | `glyph_embedder` 10.1% |
| ③ | **g 的逐层注入（4 层）** | `adaln`: `x*(1+s)+t`（zero-init）／ `xattn`: `ZeroCrossAttention` | 当前默认 adaln | 4 层合计 7.7% |
| ④ | `CalligStyleCrossAttn`（v15b） | Q=骨架 token + 2D sincos，K/V=K 个风格 token，zero-init | 已实现，v15b 测过 | — |
| ⑤ | `style_ctx_every_layer`（v15c） | K token 拼进**每层** xattn 的 context | 已实现，v15c 测过 | — |

**梯度诊断（09-22，step 212k）**：
```
blocks=0.347  glyph_embedder=0.101  inj_out_proj=0.077  other=0.129
inj|out|: L0=6.5  L1=9.6  L2=17.7  L3=60.3
```
⚠ **g 相关合计 27%**（推翻了历史的"~2.7%"，那个诊断是 v11/v12 时代做的）。
⚠ **4 个注入层极不平衡**：L3 是 L0 的 **9.2 倍**。

---

## 2. ⚠ 先拆掉一个历史误判：xattn 不是"没用"，是"被测不出来"

### 2.1 CFG 下的结构性抵消（doc 67）

```python
# forward_with_cfg
g2 = torch.cat([g, g], dim=0)      # ← 两半给同一个 g
eps = eps_uncond + cfg · (eps_cond − eps_uncond)
```

**任何"只由 g 驱动"的通路，在 `(cond − uncond)` 里完全抵消，对 CFG 的贡献恒为 0。**

xattn 的 Q = x、K/V = g_tok → **完全由 g 驱动** → 在 cfg > 1 的评测下被结构性绕过。

→ doc 54 的"**xattn strict ≈ 0**"是**评测口径的产物**，不是 xattn 本身没用。

### 2.2 xattn 的 Q 没有位置编码（真实实现缺陷）

`ZeroCrossAttention` 原实现**只给 K/V 加 sincos，Q 没有**。
而 `rope=True` 时 x 的残差流不加绝对位置 → **Q 完全无位置信息**
→ 所谓"空间寻址"退化成**内容寻址**（只能靠内容相似度猜该看哪个骨架 token）。

已加 `xattn_q_pos` 开关（默认 False 保 ckpt 兼容）；**`v15c` 开了 `xattn_q_pos=True`**。

### 2.3 连带影响：CFG 的 uncond 分支根本没训过

`dit.py` 的 drop-guard 元组**漏了 `factorized_cat`** → 训练全程零条件 dropout
→ `null_embed` 停在随机初始化 → 推理时 CFG 的 uncond 半用的是**未训练**的向量。

**该 bug 2026-09-17 才修** → 此前所有 `cat` 系 run（v12 全系、v13~v15）的 **CFG 评测口径不可信**。

### 2.4 所以

> **组件有没有用，不应该依赖一个推理期超参。**
> → 组件结论必须用 **cfg = 1.0（纯条件）** 作为默认报告口径，把 CFG 当独立旋钮单独扫。

---

## 3. 同代价对照：实测数据

| 通路组合 | ckpt | strict ssim（墨迹口径） | ink_ssim | 相对成本 |
|---|---|---|---|---|
| **adaLN（基线）** | v13_base | **0.5711** | **0.3872** | 1.00× |
| adaLN + K-token mean | v15a | 0.5488 | 0.3599 | 1.00× |
| + `CalligStyleCrossAttn` | v15b | 0.5504 | 0.3664 | ~1.02× |
| **xattn（每层 ctx）** | v15c | 0.5464 | **0.3562** ✗ | **1.33×** |

**效应量对比**：
```
注入方式之间：  |0.5504 − 0.5488| = 0.0016     (v15b − v15a，加 CA)
                |0.5464 − 0.5488| = 0.0024     (v15c − v15a，换 xattn，还是负的)
表 / 数据之间： |0.5711 − 0.5488| = 0.0223     (v13  − v15a)
大杠杆：        |0.7326 − 0.5680| = 0.1646     (实例骨架 − 标准骨架)
```

→ **注入方式的效应量比"表/数据"小一个数量级，比大杠杆小两个数量级，且落在噪声内。**

⚠ 三个 v15 变体的步数不完全对齐（v15c 的 batch 260 vs a/b 的 360，同 step 样本数少 28%），
所以上表只能看**区间**，但区间已经足够说明问题：**换注入方式买不到 0.01 以上**。

---

## 4. 机制分析：为什么 adaLN 才是对的（对风格而言）

| 维度 | adaLN | cross-attn |
|---|---|---|
| 语义 | **全局仿射调制**：整字一致的 scale/shift/gate | **逐位置内容寻址**：每个位置独立聚合 |
| 风格的属性 | 笔法（中侧锋）、墨色、粗细、节奏 —— **整字一致** | 结体、笔画位置 —— **随字而变** |
| 匹配度 | ✅ **高** | ❌ 过参数化：风格没有"逐位置"的结构 |
| 参数/block | 6×D×D 共享的 `adaLN_modulation`（本就有，风格零额外成本） | 4×D² + 每 token attention（**新增**） |
| 计算 | O(N·D) | O(N·D + N·Nc·D) |
| 退化风险 | 低 | **高**：若所有位置的 attention 权重趋同 → 退化成 adaLN，但贵 33% |
| 本仓库实测 | 最优 ckpt 用它 | 独立口径**最差**，成本 +33% |

**核心判断**：风格是**全局**的，内容是**空间**的。
- 风格 → adaLN（全局）✅
- 内容 `g` → 空间寻址（xattn 或 FiLM）✅
- 把风格塞进 xattn = 让全局属性去过一套空间机制 → **多花钱买不到东西**。

⚠ 唯一例外：**风格 × 位置的交互**是存在的（起笔/收笔、偏旁间的处理差异）。
这正是 v15b 的 `CalligStyleCrossAttn` 想做的 —— 但它实测只值 **+0.0016**，
说明**当前的风格 token 里没有"可寻址的不同模态"**（K 个 token 间余弦 0.884，几乎是同一个）。
**瓶颈在 token 的内容，不在寻址机制。**

---

## 5. 推荐方案：不换，**加**两条廉价通路

### 5.1 通路 B1：书体 FiLM on `g_tok`（结构轴）

```python
# 书体是"结构"语义，与 g 同域；g 已按书体渲染，冗余度高 → 用交互而非加数
g_tok = self.glyph_embedder(g)                    # (B, 256, D)
γ, β  = self.script_film(E_script[s]).chunk(2, -1)   # (B, D) ×2, zero-init
g_tok = g_tok * (1 + γ.unsqueeze(1)) + β.unsqueeze(1) * keep_mask
```

- 成本：`3×64`（表）+ `Linear(64 → 2D)` = **~50K 参数**，FLOPs 可忽略
- zero-init → step 0 恒等，**可安全 resume 任何已有 ckpt**
- ⚠ **加性 β 必须乘 `keep`**（否则污染 drop-g 分支，见 `20_style_encoding.md` §5.3 坑 1）

**升级版（可选）**：逐位置 FiLM —— `γ/β` 依赖 `g_tok` 的空间位置
（`γ = f(E_script, pos_embed)`），成本升到 `2×256×D ≈ 196K`，能表达"隶书压扁横向"。

### 5.2 通路 B2：风格 FiLM on `g_tok`（风格轴的空间化）

```python
# 让"风格"以空间方式影响"结构"，但成本是逐元素仿射而非 attention
γ, β = self.style_film(e_style).chunk(2, -1)      # e_style 来自 adaLN 那条通路
g_tok = g_tok * (1 + γ.unsqueeze(1)) + β.unsqueeze(1) * keep_mask
```

- 成本：`Linear(128 → 2×384)` = **~98K**（低秩 r=64 则 ~33K）
- 对比 `CalligStyleCrossAttn`（4×D² = 590K + 每 token attention）：**便宜 3–5 倍**
- 与 CA 的语义差别：
  | | FiLM（B2） | CalligStyleCA（v15b） |
  |---|---|---|
  | 表达力 | 整体仿射（"整体加粗"） | 每位置在 K 个模态间寻址（"起笔用模态1、收笔用模态2"） |
  | 依赖 | 只需要一个风格向量 | **需要 K 个真正不同的风格模态** |
  | 现状 | 可直接做 | K 个 token 余弦 0.884 → 模态是假的 → 寻址无意义 |

→ **先做 B2（便宜、验证"风格到底该不该空间化"），B2 有效且 K token 真的分化了再上 CA。**

### 5.3 通路 B3（可选，不要默认）：每层 style token xattn

即 `style_ctx_every_layer=true`（v15c）。
**保留为对照开关，不做默认** —— 成本 +33%、batch 被迫从 360 降到 240–260、独立口径最差。

### 5.4 顺手修：注入层梯度不平衡

```
现状：glyph_inject_layers=4，梯度 L0=6.5 / L1=9.6 / L2=17.7 / L3=60.3
```
L3 吃掉 60% → 前面三层几乎白训。三个候选修法（成本都低）：

| 修法 | 做法 | 代价 |
|---|---|---|
| a) 均匀铺开 | `glyph_inject_layers=12`（每层都注入） | +2 倍注入参数（~0.6M），显存 +~1.8G |
| b) 换层位 | 从"均匀分布"改成"前 4 层"（L0–L3 现在是均匀取 4 个位置） | 0 |
| c) 加 norm | 每个注入输出前加 LayerNorm，防止最后一层幅度跑飞 | ~1K 参数 |

**推荐先做 (c) + (b) 的消融**（0 成本），(a) 作为最后手段。

---

## 6. 诊断实验（**先做，不训练，1 天内可完成**）

| # | 诊断 | 怎么做 | 回答什么 |
|---|---|---|---|
| **D1** | **style 在 adaLN 里的可见性** | 测 `‖t_emb‖` vs `‖y_emb‖`；把 `y_emb` 置零，看 `adaLN_modulation(c)` 输出的相对变化量 | 若 `‖y_emb‖/‖t_emb‖ ≪ 1` → **风格被 timestep 淹没**，这才是 `ratio_style` 低的机制原因 |
| **D2** | **cfg=1.0 口径重测** | 用已有 ckpt（v13_base / v15a / v15b / v15c）跑 cfg=1.0 的 strict | 拆掉 CFG 抵消的污染，重判"注入方式有没有用" |
| **D3** | **风格的空间性** | 固定噪声 + 固定字，只换书家，把生成差图画成热力图：差异是**均匀分布**（全局）还是**集中在笔画/起收笔**（局部） | 均匀分布 → adaLN 够了；局部集中 → 该上 B2/CA |
| **D4** | **ratio_style 同口径重测** | cfg=1.0、≥100 对，对全部已有 ckpt 重跑 | 现在 1.21 / 1.42 是不同 cfg 混的，不可比 |

**D1 是我最想看的一个** —— 如果 `y_emb` 相对 `t_emb` 太小，那"风格控制是假的"就有一个
非常具体、非常便宜的修法（放大 `callig_scale`，或把 style 送进**独立于 t 的**调制通路），
比换 xattn 划算得多。

---

## 7. 如果要做内容轴 CFG（可选，最后做）

现在 `glyph_drop_prob = 0` 的 config 用不了双轴 CFG；v15 是 0.1（可用）。

```python
# forward_with_2axis_cfg 现成实现对我们不适用：
#   它把 "glyph 轴" 定义在 y_char 上，而我们是 no_char_cond=true（内容来自 g）
#   → glyph 轴 = no-op，g 仍在 4 个 pass 里完全相同 → 仍然抵消
# 要改：把 glyph 轴从 y_char 改到 g（置零或换成空骨架）
```

代价：4× NFE。**属于推理期增强，不影响任何组件结论，最后再加。**

---

## 8. 决策表

| 动作 | 做 / 不做 | 理由 |
|---|---|---|
| 保留 adaLN 作为风格主通道 | ✅ **做** | 语义正确、零成本、已验证最优 |
| 把 adaLN 换成 xattn | ❌ **不做** | 效应量 ≤0.008、成本 +33%、独立口径最差 |
| 加 B1 书体 FiLM on g | ✅ **做**（先消融） | ~50K 参数，zero-init 可回退，填补 `script_id` 完全没用的空白 |
| 加 B2 风格 FiLM on g | ✅ **做**（B1 之后） | ~98K，比 CA 便宜 3–5 倍，直接回答"风格该不该空间化" |
| `CalligStyleCrossAttn`（v15b） | ⚠ **搁置** | K token 余弦 0.884 → 模态是假的，寻址无意义；等表有信息再说 |
| 每层 style token xattn（v15c） | ❌ **不做默认**，保留开关 | +33% 成本，独立口径最差 |
| 修注入层梯度不平衡 | ✅ **做**（c + b 消融，0 成本） | L3 吃 60% 明显不合理 |
| D1–D4 诊断 | ✅ **先做** | 不训练，1 天内出结果，能省掉一次 150k 步的错误投入 |
| 内容轴 CFG | ⏸ 最后 | 4× NFE，不影响组件结论 |
