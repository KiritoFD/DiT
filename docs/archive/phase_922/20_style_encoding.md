# 922 / 20 — 风格编码：表怎么设计，要不要三层分级

> 本文只回答一个问题：**风格条件那张"表"该长什么样。**
> 注入方式（adaLN / cross-attn）见 [`30_injection.md`](30_injection.md)。

---

## 0. 结论先行

| 问题 | 回答 |
|---|---|
| 要三层分级吗？ | **要，但要的是"语义分解"不是"标签细分"。** |
| 哪三层？ | **书体（结构）→ 书家（笔法）→ 交互残差（该书家在该书体下的面貌）**，子风格 token 是第四层（可选）。 |
| 三层各走什么通路？ | **书体 → 调制骨架 g（不进 adaLN）；书家 → adaLN 全局；pair → 书家向量的加性残差（可置零回退）；K token → 可选。** |
| 判据是什么？ | **不是 strict ssim**（会零增益）。是 ① **few-shot 新书家的上限** ② **风格可分性探针** ③ `ratio_style`。 |
| 最大风险 | **"表里的向量从哪来"还没解决**。DINO 对书家几乎不区分（质心余弦 0.956）。**在表有信息之前，任何表结构都是在噪声上做设计。** |

---

## 1. 现状解剖

### 1.1 现在的表

```
v13: y_callig_embedder = LabelEmbedder(45, 128)      冻结
     c = t_emb + cond_fusion(cat([e_callig(128), e_glyph_vec(128)]))
     → 12 个 block 的 adaLN（scale/shift/gate 各 2 组）

v15: y_callig_embedder = MultiStyleEmbedder(87, K=4, 384)   可训
     e_callig = style_tokens.mean(dim=1)     # ← 注意：K=4 在进 adaLN 前被池化了
     → 每行有效容量 384 维（不是 1536），K 的作用只是"初始化时把模态分开"
```

### 1.2 数据里**存在但完全没用上**的层级

| 层级 | 类别数 | 数据里有吗 | 用作条件了吗 |
|---|---|---|---|
| **书体 script** | 3（楷 / 行 / 隶） | ✅ `script_id` 列 | ❌ **完全没有** |
| **书家 calligrapher** | 45 | ✅ | ✅（v13）/ 间接（v15 用 pair） |
| **书家×书体 pair** | 87 | ✅ `callig_script_map` | ✅（v14/v15） |
| 子风格（早/晚期、不同作品） | — | ❌ 无标注 | ⚠ v15 用 DINO K-Means 猜（失败） |

⚠ **`script_id` 目前只用于两处**：① 渲染对应书体的标准骨架 `g`；② 组 pair 词表。
**它从来没作为一个条件因子进过模型** —— 这是最明显的空白。

### 1.3 三条硬证据说明"表"是瓶颈

| 证据 | 数字 |
|---|---|
| 书家间 strict 与样本数**负相关** | **r = −0.62**（苏轼 3131 张 → 0.3485 最差；李邕 1168 张 → 0.6300 最好） |
| 多书体纠缠 | **32/45（71%）书家跨多书体** |
| 风格不可控 | `ratio_style` **1.21–2.48**（理想 5–10）= 换书家 ≈ 换噪声 |

---

## 2. 四次失败的根因归类（关键）

| 实验 | 改了什么 | 结果 | 属于哪一类失败 |
|---|---|---|---|
| v14 | 45 → 87（**标签粒度**） | 零增益 | **表结构** |
| v15a | 87 × K=4（**容量**） | 零增益 | **表结构** |
| v15a/b/c | 换注入方式 | ≤0.008 | 注入（见 30） |
| — | DINO K-Means 初始化 | 行间余弦 **0.860** | **表信息源** ✗ |

**归类结论**：

```
表结构  （粒度 / 容量）→ 已两次证伪，零增益
表信息源（向量从哪来）→ 才是真根因：
     DINO CLS pair 质心间余弦 = 0.956   ← DINO 捕捉的是"这是书法图"，不是"这是谁的风格"
     K-Means 表行间余弦       = 0.860   ← 87 行几乎共线
     同一 pair 内 4 子风格     = 0.884   ← "多风格"也是假的
     v13/v14 的健康几何(0.020)  ← 纯标签驱动（DINO 锚定因提取 bug 从未生效）
```

**→ 在"表里有信息"之前讨论表结构，是在噪声上做设计。**
SupCon（纯 pair 标签，关掉 DINO 锚定）能做出余弦 **0.0019** 的表，但 `v15b_supcon` 的终值还没跑出来。

---

## 3. "三层分级"到底有哪几种做法

先消歧义 —— "三层"至少有三种完全不同的含义，**它们的判据和预期都不同**：

| 方案 | 层级定义 | 本质 | 已被测过吗 |
|---|---|---|---|
| **A. 标签细分** | 书家(45) → pair(87) → 子风格(K) | 同一语义轴上越分越细 | ✅ v14 + v15a，**零增益** |
| **B. 语义分解** | 书体 / 书家 / 交互残差 | **三个正交语义轴** | ❌ **从未测过** |
| **C. 实例编码器** | 查表 → 给几张图编码成风格向量 | 闭集 → **开集** | ❌ 从未测过 |

**本文推荐 B，并把 C 列为 B 也失败后的方向。A 不要再试。**

---

## 4. 论证：为什么 B（语义分解）值得做

### 4.1 A 失败不等于 B 失败

v14 的 45→87 是**同一语义轴（书家）上的细分**。零增益是**预期的** ——
strict ssim 已经在标签粒度上饱和（12ch 后训练 20k 步只涨 +0.0009）。

B 的价值**不在"更细"，在"可分解"**。它带来三个 A 没有的能力：

| 能力 | 现状（87 平表） | B（三层分解） |
|---|---|---|
| **稀疏 pair 回退**（15 个 pair <50 样本，min=1） | 预训练时用"书家级质心回退"硬编码 | **训练时自动**：残差学不动 → 主效应兜底 |
| **few-shot 新书家** | 新加 **1536 维** pair 行（混杂了它从未见过的书体交互） | **只学 128 维 `E_callig` 新行**，pair 残差 = 0 |
| **未见 (书家,书体) 组合** | 词表里没有 → 无法表示 | **可合成**：`E_script[隶] + E_callig[沈周]` |

第三条是**能力上的跃迁**（不是指标微调）：现在"沈周写隶书"这个组合模型根本没法学，
分解后可以合成（真实性另说，但至少可生成、可判读）。

### 4.2 B 与 few-shot 实验直接对接（当前最活跃的实验）

09-21 的 few-shot 实测暴露的问题，恰好是 B 能解的：

| few-shot 实测 | 说明 | B 怎么解 |
|---|---|---|
| 零梯度直写只值训好行的 **~10%**（0.5229 vs 0.6342） | 新行的数值语义要与主干协同适应 | 新行维度从 1536 → **128**，且不含书体交互项 → 搜索空间小得多 |
| **50 张已饱和**（200 张反而 −0.0034，t=−4.30） | 不是数据量问题 | 与"只有一行可训"的设定一致 → 减少该行的语义负担 |
| `row_pt` > `mean_scaled`（5/5，t=+3.4~+7.0） | 行的"写法"有讲究 | 主效应行的初始化有明确语义（书家质心），pair 项从零起步 |

### 4.3 语义上的分工是清晰的

| 层级 | 决定什么 | 空间性 | 该走哪条通路 |
|---|---|---|---|
| **书体**（楷/行/隶） | 结体（隶书扁、楷书方）、笔画形态、章法 | **结构层，与 g 同域** | **调制 g**（不进 adaLN） |
| **书家** | 中锋/侧锋、粗细、墨色、连笔、节奏 | **全局**（整字一致） | **adaLN**（全局调制） |
| **pair 残差** | 该书家在该书体下的具体面貌 | 全局 | 加到书家向量上（残差） |
| **子风格 K** | 同一 pair 内多模态（早/晚期、不同作品） | 内容寻址 | 可选：mean→adaLN 或 CA |

⚠ **书体为什么不进 adaLN**：`g` 是**按书体渲染**的标准骨架（`std_path` 对应的字体），
书体信息与 `g` **高度冗余**。独立进 adaLN 会变成"同一个信号喂两遍"（12ch 失败的教训）。
正确做法是让它**与 g 交互**（调制），而不是平铺成另一个加数。

---

## 5. 推荐方案 S2：三层语义分解（具体配方）

### 5.1 结构

```python
class HierStyleEmbedder(nn.Module):
    """三层语义分解：书体(结构) / 书家(笔法) / pair(交互残差)。"""
    def __init__(self, n_script, n_callig, n_pair, d_script=64, d_style=128):
        self.E_script = nn.Embedding(n_script, d_script)      #   3 ×  64  =   192
        self.E_callig = nn.Embedding(n_callig, d_style)       #  45 × 128  =  5760
        self.E_pair   = nn.Embedding(n_pair,   d_style)       #  87 × 128  = 11136
        self.alpha    = nn.Parameter(torch.tensor(0.1))       # 残差门控，init 小
        self.film     = nn.Linear(d_script, 2 * D_g)          # 书体 → γ/β，zero-init
        nn.init.zeros_(self.film.weight); nn.init.zeros_(self.film.bias)

    def forward(self, pair_id, callig_id, script_id):
        e_c = self.E_callig(callig_id)                        # (B,128) 笔法主效应
        e_p = self.E_pair(pair_id) * self.alpha               # (B,128) 交互残差（可为 0）
        e_s = self.E_script(script_id)                        # (B, 64) 书体
        return e_c + e_p, e_s
```

**参数量 ≈ 17K + FiLM 头**（对比现状 87×1536 = **133K**）→ **参数更少，容量更大**。

### 5.2 两条通路

```
① 笔法通路（全局，adaLN）
   e_style = E_callig[c] + α·E_pair[p]
   c_vec   = t_emb + cond_fusion( LayerNorm( cat([e_style, e_glyph_vec]) ) )
   → 12 个 block 的 adaLN

② 书体通路（结构，调制 g）
   g_tok = glyph_embedder(g)                       # (B, 256, D)
   γ, β  = self.film(E_script[s]).chunk(2, -1)     # (B, D) / (B, D)
   g_tok = g_tok * (1 + γ) + β                     # 逐 token 共享的仿射
```

### 5.3 ⚠ 实现时的两个必踩坑（先写下来）

**坑 1：加性 β 会污染 drop-g 分支。**
`forward` 里有 `g_tok = g_tok * keep`（keep = 未被 glyph_drop 的样本）。
若 FiLM 出现在 keep **之前**，被丢弃的样本 `g_tok=0` 会变成 `0*(1+γ)+β = β ≠ 0` →
**"无条件"分支被重新变成有条件**（这正是 `callig_style_ca` 那段注释在防的事）。

```python
# 正确顺序：FiLM 放在 keep 之后，或 β 也乘 keep
if keep is not None:
    g_tok = g_tok * keep.view(-1,1,1)
g_tok = g_tok * (1 + γ.unsqueeze(1)) + β.unsqueeze(1) * keep.view(-1,1,1)
```
乘性 γ 不受影响（`0*(1+γ)=0`），只有加性 β 需要保护。

**坑 2：pair 残差的初始化。**
`α` init 0.1 且 `E_pair` 用 **zero-init**（不是随机）→ step 0 时退化为纯主效应，
等价于"书家表 + 无交互"，是**已验证的 v13 配方的超集**，可安全 resume 对照。

### 5.4 初始化（决定成败）

| 层级 | 初始化 | 理由 |
|---|---|---|
| `E_script` | 随机 N(0, 0.02) | 只有 3 类，随便学 |
| `E_callig` | **SupCon 预训练（书家标签）** | 已有成熟脚本；v13 的表就是这么来的（cos 0.020） |
| `E_pair` | **zero-init** | 残差语义；稀疏 pair 学不动就自动为 0 |
| `E_pair`（若非零初始化） | 层级 SupCon | 复用 `tools/pretrain_callig_script_emb.py` 的层级加权正对矩阵 |
| `α` | 0.1 | 让主效应先站稳 |

⚠ **不要再给 `E_callig` / `E_pair` 加 DINO 锚定**（`--w-anchor 0`）。
DINO pair 质心余弦 0.956，锚定只会把表往"无区分"拉（09-21 已验证）。

### 5.5 与 v15 K-token 的关系

K=4 子风格**保留为可选的正交开关**，但要注意：
`MultiStyleEmbedder` 进 adaLN 前被 mean pooling → **每行有效容量只有 384 维**。
若走 S2，建议把 K token 挂在 **pair 层**（`E_pair` → K 个 token），
因为"多模态"本来就是 pair 内部的属性（苏轼跨早晚期），不是书家级的。

---

## 6. 前置诊断（**先做这些，成本近乎为零**）

在投入一次 150k 步训练之前，用已有 ckpt 回答三个问题：

| # | 诊断 | 怎么做 | 回答什么 |
|---|---|---|---|
| **T0** | 表有没有在学 | 比 `E_callig` 的**终值 vs 初始化**的余弦与范数变化 | 冻结表（v13）不动是设计；可训表（v15）若几乎不动 = 梯度到不了 |
| **T1** | 风格有没有被编码进图 | 对同一字、只换书家生成 N 张，测**笔画级统计量**（墨色直方图、笔画宽度分布、边缘锐度）的组间/组内方差比 | 比 `ratio_style` 更可解释：**如果连笔画粗细都没变，风格就是没进去** |
| **T2** | ★ **风格可分性探针** | 用生成图提 DINO 特征，训练一个 87 类（或 45 类）分类器，测 **top-1 准确率** | **最直接**：随机基线 1.1%（87 类）。若 ≈ 基线 → 生成的书家根本不可分；若能到 30%+ → 风格确实进去了，问题在别处 |

**T2 是我最推荐的判据** —— `ratio_style` 受 intra/inter 定义与 cfg 影响太大，
分类探针直接测"能不能认出是谁写的"，且**跨 ckpt 可比、不需要同 cfg**。

---

## 7. 判据：不要用 strict ssim

| 判据 | 现状基线 | 目标 | 为什么用它 |
|---|---|---|---|
| ★ **风格可分性探针（T2）** | 待测（随机 1.1%） | **> 20%**（45 类） | 直接量化"生成的风格可分" |
| ★ **few-shot 新书家 Δssim** | +0.010 ~ +0.038（50 张） | **冻结主干下 ≥ +0.05** | 直接测"表能不能装下新风格" |
| `ratio_style`（cfg=1.0，≥100 对） | 1.21–2.48 | **≥ 2.5** | 风格控制是否真实 |
| `ink_ssim`（strict 249） | 0.3898（最优） | — | 兜底：不能变差 |
| ~~strict ssim~~ | 0.573 | — | ✗ **已饱和**，效应量 < 0.01，用它判不出差别 |

⚠ **如果 S2 在 strict ssim 上零增益、但在 T2 / few-shot 上明显涨 —— 这就是成功了。**
反过来若只有 ssim 涨而 T2 不动，那是噪声。

---

## 8. 风险与不做清单

| 风险 | 概率 | 应对 |
|---|---|---|
| **书体层与 g 高度冗余 → 学不到东西** | 中（g 已按书体渲染） | 先跑一次"只加书体 FiLM"的消融（成本 1 天），无效就砍掉这一层 |
| **表信息源没解决 → S2 也空转** | **高** | **必须先等 `v15b_supcon` 终值**。若 SupCon 表也不涨 → 直接跳到 C（实例编码器） |
| 稀疏 pair 的残差学到噪声 | 中 | `min_samples=50` 以下的 pair 残差**强制置零**（`E_pair` 分两块，稀疏块冻结为 0） |
| 三层的维度分配不合理 | 低 | 书体 64 / 书家 128 / pair 128 是保守值，可扫 |

**不做**：
- ✗ 继续细分标签粒度（A 方案，已两次证伪）
- ✗ 给表加 DINO 锚定（DINO 不区分书家）
- ✗ 用更多子风格 token（K=8/16）救容量（K=4 已经零增益）

---

## 9. 方向 C：实例风格编码器（B 也失败后再走）

如果 `v15b_supcon` 与 S2 都证明"书家 ID 能提供的监督已经用尽"，就该换范式：

```
现在：  style = Table[callig_id]                  ← 闭集，信息量 = 标签数
C：     style = StyleEncoder(几张该书家的图)        ← 开集，信息量 = 实例特征
```

**支持这个方向的证据**：
- 实例骨架条件（GT 骨架）0.7326 vs 标准骨架 0.5680（**+0.16**）→ 实例级信息**确实有效**
- few-shot 用 50 张就能把新书家学到 0.557（接近全量平均 0.568）→ 50 张图里**有足够信号**
- 米芾被真训出来的行 0.6342 ≫ 直写 0.5229 → 从实例学比从特征构造好得多

**代价**：改变任务设定（推理时要给参考图），且需要重训主干。
**建议**：作为 S2 之后的第二阶段，不要和 S2 并行。
