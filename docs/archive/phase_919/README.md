# docs/919 — 项目全量快照（2026-09-19）

**这是一次不接续 `docs/system/00-73` 编号的完整快照。**
读完这 6 份文件即可完整接手本项目（数据 / 模型 / 训练 / 结果 / 基础设施）。

| 文件 | 内容 | 关键词 |
|---|---|---|
| **[00_overview.md](00_overview.md)** | **从这里开始** —— 目标、结论清单、核心问题、活跃实验、下一步 | 一页速览 |
| [10_data.md](10_data.md) | 数据资产全谱：三代数据集、px60 构建、评测集口径、派生资产（骨架/风格表/DINO）、8 个数据坑 | **口径警告** |
| [20_model.md](20_model.md) | DiT-2Cond 架构、三个条件入口、CFG 实现与 bug、已证伪组件的**机制**解释 | 条件机制 |
| [30_training.md](30_training.md) | 标准配方、v10→v14 实验链、长 epoch 重构、10 个训练坑 | 配方 |
| [40_results.md](40_results.md) | 全部结果表、多样性指标、**逐书家分析**、7 条解读纪律 | **数据** |
| [50_infra.md](50_infra.md) | 环境、torch.compile 事故、性能/MFU/VRAM、运维手册 | 基础设施 |

## 三句话摘要

1. **strict ssim 0.5703 / LPIPS 0.364**（未见的 (书家×字) 组合）—— 已饱和，容量/深度/宽度/12ch/xattn/87 表/全解冻全部无法突破。
2. 真正的瓶颈是**风格可控性**（`ratio_style ≈ 1.2–2.5`，理想 5–10）+ **书家条件容量**（每类 1 个 128 维向量装不下多模态风格；r(样本数, strict) = **−0.62**）。另查明旧 SupCon 表的 DINO 锚定因特征提取 bug **从未生效**（`10_data.md` §3.4）。
3. **正在跑（2026-09-19 深夜起）**：v15 多模态风格矩阵 —— 87 pair × K=4 token（DINO K-Means 质心初始化 + λ=0.01 mean 锚定）× 三种注入方式（池化→adaLN / CA 书家化 / 每层 ctx），**全部从头训练**各 150k 步，串行 ~26h；判据 = strict/seen 对比 + `ratio_style`。

## 与旧文档的关系

- `docs/system/53–73` 是**按日期分篇的连续工作日志**（含大量单点细节与推导）。
- `docs/919/` 是**同一信息的重新组织**：按主题（data/model/training/results/infra）聚合，
  并做了**交叉一致性核对**（例如：40_results.md §1.2 曾发现"v13_base 与 v13_wd01 的
  100k/115k 两点完全同值"，追查后实为**口径陷阱**——base 的 in-training strict 全程 n=50，
  wd01 是 n=250，"同值"是误把 wd01 的行标成 base；已用 per-sample batch CSV 重做同口径对比）。
- 引用旧文档时仍以 `docs/system/NN_*.md` 为准（本目录里凡引用处都写了编号）。