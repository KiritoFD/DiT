# 交接文档：书法 DiT 生成（稀疏组合条件）

> 交接时间：2026-08-15 21:00  
> 远程：`root@10.176.54.17:36430`，项目 `/root/Workspace/xy/DiT`，Python `/opt/conda/bin/python`  
> 本地工作区：`G:\GitHub\DiT`（docs/ 设计文档与实验记录在此）  
> 远程无外网（不能 pip/huggingface 下载）。SSH 偶发不稳定，命令失败就重试。

---

## 0. 任务目标与当前状态（先读这个）

**目标**：生成以「书家 calligrapher × 字形 glyph」为条件的书法字（256×256，latent DiT），
重点解决稀疏组合（0.264% 覆盖）下的泛化。用户最终决策：

1. **script 不是独立第三因子** → `glyph = script×character` 合并为一个类（`glyph_id = script_id×7026 + character_id`，共 35130 类，活跃 21495）。
2. **模型**：`DiT-2Cond-S/2`（depth=12, hidden=384, heads=6, patch=2）+ `factorized_add`
   条件（callig 128 + glyph 192 → 投影相加），39.58M 参数，**从头训练**（不 warm start V1 旧权重）。
3. **V3-A 可组合 score**：4-way mask 训练（full 60% / callig-only 15% / glyph-only 15% / uncond 10%），
   采样时既可 full CFG，也可 pair-composed（`s_A + s_G − s_0`）。
4. **结构损失**（最新指示，2026-08-15 19:51）：**不用 subset**，主 batch=8，
   **每张都算 pixel canny + skeleton loss**，两个权重拉高到 **w_canny=1.0, w_skel=1.0**。

**当前正在跑的训练**（不要动它）：

| 项 | 值 |
|---|---|
| 配置 | `exp_s_5script_v3a_glyph_cs.json`（远程根目录） |
| 日志 | `exp_v3a_glyph_cs.log` |
| PID | 336762（19:57 启动） |
| 结果目录 | `5script/results/v3a/20260815-195729-v3a-2factor-glyph-canny-skel/` |
| 进度 | step ~1220+（1.68 steps/s，约 6.5h 跑完 39600 步） |
| 显存 | 15.69G 峰值（24.5G 卡，安全） |
| 首个 auto-eval | step 1000：MSE=2.324 / SSIM=0.091（EMA 尚在 warmup，正常） |
| 训练 loss 趋势 | Total 1.54→0.80，Diff 0.68→0.20，Canny 0.20→0.13，Skel 0.66→0.47 |

**下一步该做的**（按顺序）：
1. 等 step 3000/5000 的 auto-eval，看 free-sampling MSE/SSIM 是否持续下降（对比：无结构损失版 step2000 MSE=1.699/SSIM=0.271；V1-20k 基线 MSE=1.005/SSIM=0.458）。
2. 训练结束后用 `eval_compose.py`（**尚未完成，见 §7 注意事项**）做 pair-composed 对比评估，判定 V3-A 是否有效。
3. 设计文档后续路线：V3-B 条件图 adapter → V4 空间字形 + 参考风格（见 docs）。

---

## 1. 数据与准备（已就绪，勿重做）

- `5script/train.csv`（147,841 行）/ `test.csv`（4653）/ `eval.csv`（472）/ `fast100.csv`（100），
  均已加 `glyph_id` 列（= `script_id*7026 + character_id`）。
- `5script/eval_strata/`：`clean_unseen_triple_100.csv`（auto-eval 用）、`seen_triple_100.csv`、`unseen_pair_100.csv`，均含 glyph_id。
- `5script/id_maps.json`：calligrapher/script/character 的 id 映射。
- latent 缓存：`final_latents/shard_*.npz`（32×32×4，scale 0.18215）；图像 `final_imgs_256/`；
  **canny GT `final_canny/`、skeleton GT `final_skeleton/`（各 329,715 张，5script 图 100% 覆盖）**。
- 采样器：`DistributedFactorBalancedSampler`（glyph α=0.35，callig α=0.15）——已改造成按 glyph_id 计数。

## 2. 已完成的代码修改（2026-08-15 19:30-20:00）

| 文件 | 改动 |
|---|---|
| `models.py` | `DiT_2Cond` 增加 `condition_fusion="factorized_add"` 分支（callig_proj/char_proj 低维投影相加 /√2）+ 4-way mask（`cond_drop_all_prob`/`cond_drop_one_prob`，drop-one 时 which∈{0,1} 等概率 drop callig 或 drop glyph）。`forward_with_cfg` 未变（uncond=双 num_classes）。legacy 路径完全保留。 |
| `dataset.py` / `latent_dataset.py` | `y_char` 优先读 `row.get('glyph_id', row['character_id'])`。 |
| `samplers.py` | factor-balanced 权重按 glyph_id 计数（无 glyph_id 列时回退 character_id）。 |
| `eval_auto.py` | `prepare_gen_cache`/`eval_gen_in_memory` 增加 `cond_mode="2cond"` 参数（2cond 时 mk 只含 y_callig/y_char，conds 三元组中 script 位存 -1）。 |
| `eval_gen.py` | 支持 `--cond-mode 2cond`（手动自由采样评估）。 |
| `train.py` | ① 2cond 模型构建传 factorized_add/drop 参数；② ckpt schedule：**前 5000 步每 1000 存，之后每 5000 存**（用户要求）；③ eval 调用传 cond_mode；④ 新增 `--struct-subset`（**用户已否决 subset 方案，配置里设 0 = 全量，代码保留但不用**）。 |
| `_add_glyph_col.py` | 一次性脚本，给 5script csv 加 glyph_id 列（已跑完）。 |
| `_smoke_v3a.py` | 模型 smoke test（已通过：参数 39.58M、4-way mask 比例、CFG batch 一致性）。 |
| `_check_struct_gt.py` | canny/skel GT 覆盖检查（已通过 100%）。 |

**关键 bug 历史**（不要回退）：`forward_with_cfg` batch>1 曾只复制前半批导致弃后半批，已修复为全批复制。

## 3. 训练配置速查（exp_s_5script_v3a_glyph_cs.json）

- batch=8（每张都算结构损失）；max_steps=39600；lr=1e-4 cosine warmup500 min0.1；weight_decay=0.02；
- EMA 0.9999 + warmup（early EMA 保留随机初始化是正常的）；
- `w_canny=1.0, w_skel=1.0, struct_subset=0, use_canny=true, use_skel=true, w_repa=0.0`；
- auto_eval=true：`eval_csv=5script/eval_strata/clean_unseen_triple_100.csv, eval_n=100, eval_steps=50, eval_cfg=4.0, eval_batch=16`；
- `preload=true, preload_workers=24`（启动时 ~47GB RAM 预载 latent+canny+skel，~2 分钟）；
- VAE decode 走 `grad_ckpt` + fp32 autocast（train.py `_decode`），结构损失在 pred_xstart 上算；
- 结构损失有 NaN 防护：Sobel 归一化分母 detach + clamp（losses.py 注释说明，勿改回）。

## 4. 评估与基线

- **auto-eval 是自由采样**（纯噪声→DDIM 50 步→cfg=4.0，`eval_gen_in_memory`），
  **绝不是** GT-latent 单步重建（后者曾误导结论，用户明确否决）。
- 基线（free-sampling，clean_unseen_triple_100）：V1 factorized 3 因子 20k = MSE 1.005/SSIM 0.458；
  V2（+latent 结构损失）24k = MSE 1.002/SSIM 0.450（结构损失在 latent 上被否）。
- V3-A 无结构损失版（batch 224）跑到 step2000 即被停：MSE 1.699/SSIM 0.271（记录在案，ckpt 在 `20260815-193523-.../checkpoints/0001000/0002000.pt`）。

## 5. 设计文档与实验记录（本地，需随进展更新）

- `docs/design/2026-08-15-sparse-compositional-calligraphy-dit.md`：完整方案（V3-A 已改为二因子 4-way 版；V3-B/V4 未实施）。
- `docs/experiments/2026-08-15-v3a-2factor-glyph.md`：V3-A 实验记录（当前训练已记录）。
- `docs/experiments/2026-08-15-factorized-3cond.md`：V1/V2 记录（结构损失在 latent 空间被否的证据）。

## 6. 远程文件清单（有用的一起列了）

训练/配置：`train.py`、`models.py`、`losses.py`、`dataset.py`、`latent_dataset.py`、`samplers.py`、
`eval_auto.py`、`eval_gen.py`、`eval_compose.py`（未完成）、`exp_s_5script_v3a_glyph_cs.json`、
`exp_s_5script_v3a_glyph.json`（无结构损失版，已停）。

诊断脚本（`_` 前缀，均已跑完）：`_add_glyph_col.py`、`_smoke_v3a.py`、`_check_struct_gt.py`。

旧实验：`5script/results/compositional/`（V1/V2 ckpt），`5script/results/exp_s_5script/`（legacy concat 版）。

## 7. 未完成事项与坑（重要）

1. ~~**`eval_compose.py` 未完成**~~ **已完成（2026-08-15 20:39 验证通过）**：现为每步 4 路 forward
   （full/A/G/0）→ eps 空间组合 → 反推 x0 → 确定性 DDIM 更新。用 step4000 ckpt 跑通 n=8
   （wI=0 pair MSE 2.893 vs wI=1 full MSE 2.834，早期 EMA 接近属正常）。完整对比等 ≥10k 步再跑：
   `/opt/conda/bin/python eval_compose.py --ckpt <ckpt.pt> --n 100 --out compose_eval`。
2. `struct_subset` 参数已加但用户否决 subset——**不要再用**；配置中设为 0（全量）。
3. REPA 默认权重是 1.0，配置里必须显式 `w_repa=0.0`，否则 REPA 会悄悄占 loss 大头（probe 时踩过）。
4. 远程 bash 里 `python -c` 带引号/括号会因 PowerShell 转义爆炸——一律写成 `.py` 文件再 scp/ssh 执行。
5. 改 `train.py`/`models.py` 等核心文件后，先本地 `python -m py_compile` 或 ast 语法检查再传。
6. 训练中断恢复：`--resume-full <ckpt.pt>`（会恢复 opt/scheduler/EMA/step）。ckpt 存完整 state（use_lora=false）。
7. 24.5G 卡：b224 无结构损失 19.2G；b8+全量结构损失 15.7G。VAE decode 全量在 b32 就会 OOM（21.3G），所以**结构损失配小 batch 是唯一安全组合**。
8. 远程无外网：不要尝试 pip install 或 huggingface 下载；VAE 用本地 `pretrained_models/sd-vae-ft-ema`。
9. gradio/flask 已废弃（版本地狱 + 无外网），展示用 SSH 隧道 + `eval_latest.png` 或本地 `tools/train_dashboard.html`。

## 8. 常用命令

```bash
# 看训练进度（远程）
tail -20 /root/Workspace/xy/DiT/exp_v3a_glyph_cs.log
grep auto-eval /root/Workspace/xy/DiT/exp_v3a_glyph_cs.log | tail

# 手动自由采样评估（3cond 或 2cond 通用）
/opt/conda/bin/python eval_gen.py --ckpt <ckpt.pt> --csv 5script/eval_strata/clean_unseen_triple_100.csv \
    --n 100 --steps 50 --cfg 4.0 --out gen_eval --cond-mode 2cond

# 本地拉取最新 eval 图看效果
scp -P 36430 root@10.176.54.17:/root/Workspace/xy/DiT/5script/results/v3a/<run>/checkpoints/eval_latest.png .
```

## 9. 时间线回顾（供上下文恢复）

- 13:10 legacy concat 3 因子（5script）开跑 → 25.7k 被容器重启打断，free-sampling MSE 最差 → 判定微调不够。
- 15:39 V1 factorized_add（3 因子，20k，MSE 1.005/SSIM 0.458）。
- 17:47 V2 = V1-20k 继续 + latent canny/skel（24k，MSE 1.002/SSIM 0.450）→ latent 结构损失被否。
- 19:35 V3-A 二因子 glyph（无结构损失，b224）→ step2000 停（MSE 1.699）。
- 19:51 用户指示：b8 + 每张全量 pixel canny/skel + 权重 1.0/1.0 → 当前训练（PID 336762）运行中。
- 20:38 训练 step4000：auto-eval MSE=1.933/SSIM=0.120（持续下降）。
- 20:39 eval_compose.py（pair-composed 每步组合）完成并验证通过。
- 20:40 监控前端上线：本地 `http://127.0.0.1:8833/train_dashboard.html`（pull_log.py --loop 后台 + http.server 后台）。
